# 오센틱아트 — 콘텐츠 발행 가이드 (색인·AEO)

> 공통 표준: `D:\키워드엔진\docs\콘텐츠 발행 표준.md` (**먼저 읽을 것**)
> 이 문서는 오센틱아트에만 해당하는 실측 상태와 할 일이다.

## 현재 상태 (2026-07-23 sitemap 전수 실측)

| 항목 | 값 |
|---|---|
| 발행(자사웹 블로그) | **87편** (sitemap 기준) |
| **전수 색인률** | **39% (34/87)** — 7/12 41% 대비 **실질 개선 없음** |
| 색인 실패 | **발견-미크롤 43건** · 크롤 후 색인거부 5건 · 구글모름 5건 |
| 28일 성과 | 노출 563 · 클릭 18 · 평균순위 8.0위 (노출 받은 글 31편/클릭 8편) |
| 본문 중앙값 | 1,534자 (7/12 1,056자 → 개선) · 1,500자 미만 40편 잔존 |
| 기술 기반 | robots·sitemap·JSON-LD·llms.txt·목록페이지 87링크 전부 노출 — **양호** |

### ⚠️ 엔진 보고값(69%)은 과대 — 절반만 본 수치

`index_audit.js`는 `publish_log.csv` 기반인데 오센틱아트 로그는 **43건뿐**이다.
**나머지 44편(6/8~6/25 발행분)이 통계에서 통째로 빠져 있다.** 빠진 쪽이 색인이 가장 안 된 구간이라
엔진은 69%로 보고하지만 **실제는 39%**다. `publish-quota.mjs`도 이 과대값을 읽어 발행을 허용 중이다.

### 진짜 원인: 얇은 글이 아니라 **발행 시기·내부링크** (7/12 진단 수정)

발행 시기별 색인률 — 최근 글일수록 잘 되고, **초기 롱폼이 가장 안 된다**:

| 발행 시기 | 색인률 | 특징 |
|---|---|---|
| 6/08 (초기 롱폼 22편) | **2/22 (9%)** | 3,200~5,400자 최상급 글인데 20편이 **크롤조차 안 됨** |
| 6/10~6/25 (얇은글 28편) | 8/28 (29%) | 900~1,400자 |
| 7/05~7/12 (하루 3편기) | 19/31 (61%) | 7/12 내부링크 삽입분 포함 |
| 7/13~ (게이트 적용후) | **5/6 (83%)** | 2,300~3,800자 + 내부링크 2~3 |

- **길이는 변수가 아니다**: 1,000자 미만 14% · 3,000자 이상 13%로 양극이 모두 낮다(3,000자대가 낮은 건 대부분 6/8 발행분이기 때문).
- **내부링크는 변수다**: 링크 0개 33% vs 링크 1개 이상 52%.
- 즉 **얇은 글 대량발행 제동(게이트·쿼터)은 방향이 맞았고 효과도 확인**됐다(7/13~ 83%). 남은 문제는 **오래된 글 44편이 링크·크롤 신호를 못 받는 것**이다.

### 미완료로 남은 7/12 조치

- 크롤 후 색인거부 2건(`상견례-…`·`집들이-…-한점`)은 본문을 2,200~2,500자로 보강했으나 **lastCrawl이 아직 2026-07-05** — 구글이 보강본을 못 봤다. **GSC 색인 요청(수동)이 미이행**이며, 이걸 하지 않으면 보강 효과가 판정되지 않는다.

### 관측된 이상 (원인 미상)

- 2026-07-23 09:13~09:19에 블로그 **49편의 `updated_at`이 일괄 갱신**돼 sitemap `lastmod`가 87편 중 73편이 오늘로 나갔다. 발행 배치 로그는 09:08 종료이므로 배치 산출물이 아니다. 평소(7/19~7/22)는 하루 1~6건이라 **일회성으로 보이나, 반복되면 "매일 전면 수정" 신호로 크롤 예산을 낭비**한다. 재발 여부 관찰 필요.

## 이미 적용된 것 (코드에 심어둠)

- `scripts/publish-quota.mjs` — 엔진의 색인률을 읽어 **오늘 발행 편수를 결정**(80%↑ 2편 / 60%↑ 1편 / **미달 0편 + 보강모드**). `run-daily-blog.ps1`이 이걸 먼저 호출하고, 0편이면 신규 발행을 건너뛴다.
- `scripts/check-content.mjs` — 발행 전 색인 게이트: 본문 2,000자 · 고유 수치 3개 · 내부링크 2개 · 추출형식(표/번호) · FAQ 4개. 하나라도 미달이면 Claude 재작성으로 넘어간다.
- `scripts/daily-blog-prompt.txt` — BLUF(질문형 H2 아래 완결된 답 1문장), 고유 수치, 내부링크 규칙 명시. **1,000~1,400자 상한 폐기 → 2,000자 이상**.

## 지금 해야 할 일 (2026-07-23 갱신, 우선순위)

1. **6/8 롱폼 22편 살리기 — ROI 최고.** 3,200~5,400자짜리 사이트 최상급 글 20편이 크롤조차 안 되고 놀고 있다.
   새 글을 쓰는 것보다 이미 있는 좋은 글을 색인시키는 편이 훨씬 싸다.
   - 이들은 본문 내부링크가 **0개**다. 색인된 최근 글(7/05~) → 이 22편으로 **주제가 맞는 인바운드 링크**를 건다.
   - 관련글 컴포넌트는 "최신 3 + 오래된 3"이라 22편 중 **3편만** 수혜한다. 나머지 19편은 목록페이지 링크 1개뿐(87링크로 희석).
     → 관련글을 **카테고리·주제 매칭** 또는 **미색인 우선**으로 바꾸면 코드 한 곳으로 전체가 커버된다.
2. **GSC 색인 요청(수동) — 7/12부터 미이행.** 보강한 2건은 `lastCrawl`이 아직 7/5라 **보강 효과가 아직 판정조차 되지 않았다.**
   URL 검사 → 색인 요청: 보강 2건 + 6/8 롱폼 중 우선순위 상위 10편.
3. **엔진 통계 사각지대 해소** — `publish_log.csv`에 6/8~6/25 발행분 44편이 없다.
   `import_sitemap.js`로 편입하되, **그대로 돌리면 안 된다**: publish_log는 한글 URL(22건)·인코딩 URL(4건)이 섞여 있고
   sitemap은 전부 퍼센트 인코딩이라 **중복 판정이 안 돼 43건이 이중 등록**된다. URL 정규화 후 편입할 것.
4. 색인률 회복(60%+)되면 쿼터가 자동으로 1편/일로 풀린다. 80% 넘으면 2편.
   단 현재 쿼터는 **과대값(69%)** 기준으로 1편을 내주고 있다(실제 39%).

## 2026-07-23 조치 완료

- ✅ **관련글 링크 균배(코드·배포됨)** — `blog/[slug]/page.tsx`의 `oldest`를 `slice(-3)` 고정에서 **slug 해시 기반 결정적 회전**으로 교체.
  카테고리에서 가장 오래된 3편만 링크받던 구조를 오래된 글 전체 순환 분배로 바꿨다.
  시뮬레이션 **인바운드 0인 글 58편 → 1편**, 6/8 롱폼 인바운드 0 14편 → 1편.
  commit `99d270b` → push → 라이브 검증 완료(관련글 6개가 신규 로직 예측과 정확히 일치).
- ✅ **미색인 글 인바운드 보강(DB)** — `scripts/link-boost-unindexed.mjs` 신설·적용.
  **방향을 뒤집은 것이 핵심**: 7/12에는 미색인 글"에" 링크를 넣었으나, 크롤되지 않는 페이지의 아웃바운드 링크는
  구글에 전달되지 않는다. 이번엔 **색인 확인된 31편의 본문에** 블록을 넣고 링크 대상을 미색인 글로 고정했다.
  미색인 53편 **전부**가 인바운드를 받는다(본문 3,000자+ 롱폼 우선 배분). 백업 `scripts/_tmp/backup-linkboost-20260723.json`(가역).
  결과: 본문 내부링크 보유 글 29편 → **48편**, "함께 보면 좋은 글" 블록 13편 → **44편**.
- ✅ **엔진 통계 사각지대 해소** — `scripts/sync-publish-log.mjs` 신설·적용으로 누락 44편을 `publish_log.csv`에 편입(43편은 중복 없이 정확히 제외).
  `import_sitemap.js`를 쓰지 않은 이유는 URL 완전일치 비교라 한글/인코딩 차이로 **43건이 이중 등록**되기 때문.
  발행일도 `lastmod`가 아니라 DB `published_at`을 썼다(lastmod는 본문 수정마다 바뀐다). 백업 `publish_log.bak-20260723.csv`.
- ✅ **IndexNow 99건 재제출**(빙·네이버 등).
- ✅ **엔진 재측정 → 발행 제동 정상화** — `measure.js`+`reinforce.js` 재실행 결과 **색인 31/83 (37%)**로 판정
  (sitemap 전수 39%와 일치 = 사각지대 해소 확인). `index_rate` **0.69 → 0.37** 정정.
  이에 따라 `publish-quota.mjs`가 **quota 0 · reinforce 모드**를 반환한다.
  **내일부터 자동 신규 발행이 멈추고 보강 모드로 전환된다 — 정상 동작이며, 색인률 60% 회복 시 자동으로 1편/일이 풀린다.**
- ⏳ **남은 수동 조치(대표)**: GSC URL 검사 → **색인 요청**. 7/12부터 미이행 상태이며, 보강한 2건은 `lastCrawl`이 아직 7/5라
  이걸 하지 않으면 보강·링크 효과가 판정되지 않는다. 우선순위: 크롤거부 2건 + 6/8 롱폼 상위 10편.

## 2026-07-12 조치 완료

- ✅ **크롤거부 2건 본문 보강** — 상견례(1,092→2,240자)·집들이-한점(726→2,019자). 고유수치·비교표·내부링크 5개·FAQ 유지, 게이트 통과. 운영 DB 반영·라이브 200 확인.
- ✅ **미색인 20건 내부링크 삽입** — 각 글에 "함께 보면 좋은 글"(같은 언어·주제 3링크) 추가. 발견-미크롤 13 + 구글모름 7 커버. 백업 `scripts/_tmp/backup-20260712.json`(가역).
- ✅ **관련글 로직 개선(코드)** — `blog/[slug]/page.tsx` 관련글을 "최신 3 + 오래된 3"로 확장 → 오래된·고아 글도 형제 페이지에서 인바운드 링크 수신. `pnpm build`·`tsc` 통과. **⚠️ 배포(push) 시 반영** — 대표 승인 대기(마케팅 페이지, 배포 게이트 비해당).
- ✅ **IndexNow 93건 재제출**(빙·네이버·ChatGPT). ✅ **sitemap GSC 제출 상태 정상 확인**(7/11 수집, 오류 0 — unknown 7건은 제출누락이 아니라 크롤 대기).
- ⏳ **남은 수동 조치(사용자)**: GSC URL 검사 → **색인(재크롤) 요청** — 보강한 2건 + 미색인 20건. Google 인덱싱 API는 일반 페이지 미지원이라 콘솔 UI에서 수동. 7일 후 엔진 `measure.js`가 색인 재판정.

## 하지 말 것

- **하루 3편 복구 금지.** 발행량을 늘릴수록 크롤 예산이 분산된다.
- 1,000자대 글 재도입 금지. 게이트(2,000자) 적용분이 83% 색인되는 것으로 효과가 확인됐다.
- 게이트 우회 발행 금지(`check-content.mjs` 실패를 무시하고 publish 하지 말 것).
- **`index_audit.js` 콘솔의 "브랜드별 색인률"을 믿지 말 것.** 판정이 영어(`Submitted and indexed`) 매칭인데
  API를 `languageCode: 'ko'`로 호출해 응답이 한국어라 **항상 0%로 표시**된다. CSV 원자료·`weights.json`은 정상이다.
  (같은 함정: `"색인이 생성되지 않음"`은 `"색인이 생성"`을 포함하므로 부분일치로 판정하면 미색인이 색인으로 뒤집힌다.
  판정은 `"생성되었습니다"`로 할 것.)

## 확인 명령

```bash
node scripts/publish-quota.mjs                     # 오늘 몇 편 쓸지(색인률 연동)
node scripts/audit-sitemap-index.js                # sitemap 전수 색인 감사 — 진짜 색인률(87편 약 3분)
node D:\키워드엔진\index_audit.js --brand 오센틱아트  # publish_log 기반(사각지대 44편 있음, 콘솔 색인률 표시 버그)
```
