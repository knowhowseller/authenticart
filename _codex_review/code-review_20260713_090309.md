<!-- Codex 코드리뷰 | repo: C:\Users\노하우셀러\authenticart | 생성: 2026-07-13 09:03 -->

[변경 요약]
 scripts/check-content.mjs | 25 ++++++++++++++++++++++---
 1 file changed, 22 insertions(+), 3 deletions(-)

승인 보류.

**Findings**

[High] `scripts/run-daily-blog.ps1:63-76`  
`check-content.mjs`가 실패해도 Claude 수정 1회 후 재검사 결과를 막지 않고 그대로 `publish-blog.mjs`를 실행합니다. 이번 diff의 목적이 “색인 게이트 실패 시 발행 차단”인데, 현재 흐름은 두 번째 검사도 실패해도 발행됩니다.  
영향: 글자수/수치/내부링크/FAQ 미달 콘텐츠가 자동 발행될 수 있음.  
권장: 두 번째 `check-content` 후에도 `$LASTEXITCODE -ne 0`이면 notify fail/reinforce 후 `exit 1`.

[Med] `scripts/publish-quota.mjs:26,35-40`  
`index_rate` 타입/범위 검증이 없습니다. `null`만 처리하고, `"22%"`, `"0.22"`, `NaN`, 음수 같은 값이면 `LADDER.find()`가 `undefined`가 될 수 있고 스크립트가 터집니다. `run-daily-blog.ps1`은 이 경우 fallback 1편 발행으로 가므로, 색인률 미달인데도 신규 발행할 수 있습니다.  
권장: `Number(rate)` 변환, `Number.isFinite`, `0~1` clamp/검증 후 실패 시 `quota:0` 또는 명시 정책 적용.

[Med] `scripts/check-content.mjs:42`  
“추출 가능한 형태” 판정이 너무 좁습니다. 표가 본문 맨 앞에서 시작하면 `\n\|` 조건 때문에 실패하고, 일반 불릿 `- 항목`, 체크리스트가 아닌 목록, `1)` 형식도 실패합니다.  
영향: 정상 원고가 불필요하게 Claude 재작성 루프로 갈 가능성.  
권장: `/(^|\n)\|.*\|/`, `/(^|\n)\s*(\d+[.)]|[-*])\s+/` 정도로 완화.

[Low] `scripts/check-content.mjs:38`  
내부링크 카운트는 `/blog|/classes|/artworks`만 인정하지만 CTA 허용 목록에는 `/group-request`, `/signup/instructor`, `/`가 포함됩니다. 원고가 전환 CTA 중심이면 “CTA는 정상인데 내부링크 부족”으로 잡힐 수 있습니다.  
권장: 내부링크 목적이 크롤 연결성이라면 `OK_ROUTES` 기반으로 세거나, “콘텐츠형 내부링크”와 “전환 CTA”를 별도 기준으로 명확히 분리.

[Low] `scripts/check-content.mjs:45`  
코드는 FAQ `4개 이상`을 허용하지만 메시지는 `정확히 4개`라고 출력합니다. 운영자가 잘못 이해할 수 있습니다.  
권장: 메시지를 `최소 4개`로 수정.

**보안/권한 영향**

이번 변경 범위에서는 인증/인가, RBAC, IDOR, API 권한, DB 권한을 직접 건드리지 않습니다. 보안상 즉시 차단할 이슈는 보이지 않습니다. 핵심 리스크는 자동 발행 파이프라인의 품질 게이트 우회와 색인률 기반 발행량 판단 실패입니다.

**확인한 것**

`node --check scripts/check-content.mjs`와 `node --check scripts/publish-quota.mjs`는 통과했습니다. 다만 현재 워크트리에는 제공된 diff 외에도 `run-daily-blog.ps1`, `daily-blog-prompt.txt`, `docs/` 등 추가 변경이 있어, 실제 승인 전에는 전체 diff 기준으로 한 번 더 보는 게 맞습니다.