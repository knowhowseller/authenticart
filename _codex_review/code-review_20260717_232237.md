<!-- Codex 코드리뷰 | repo: C:\Users\노하우셀러\authenticart | 생성: 2026-07-17 23:22 -->

[변경 요약]
 .claude/settings.local.json |  3 ++-
 scripts/check-content.mjs   | 25 ++++++++++++++++++++++---
 2 files changed, 24 insertions(+), 4 deletions(-)

검토 결과, **일반 보안/RBAC/IDOR/DB/API 직접 위험은 낮지만**, 에이전트 권한과 운영 게이트 품질 쪽에 실제 리스크가 있습니다.

**Findings**

[High] [.claude/settings.local.json](/C:/Users/노하우셀러/authenticart/.claude/settings.local.json:22)  
`mcp__claude_ai__transactions` 허용 추가는 민감 데이터 접근 권한으로 보입니다. “transactions”가 결제/사용량/거래 내역 계열이면 Claude가 민감한 계정·비용·거래 데이터를 조회할 수 있고, 이 파일이 추적 대상이라면 repo 공유/커밋 시 권한 정책까지 같이 전파됩니다.  
권장: **수정**. 이번 변경에서 제거하거나, 반드시 필요하면 `.local` 비추적 파일로 분리하고 최소 범위 권한인지 확인하세요. 특히 Claude 자동화가 읽은 내용을 다른 산출물에 섞어 쓰는 흐름이 있으면 개인정보/비용정보 유출면이 생깁니다.

[Med] [scripts/check-content.mjs](/C:/Users/노하우셀러/authenticart/scripts/check-content.mjs:35)  
`MIN_NUMBERS`는 “고유 수치”라고 되어 있지만 실제로는 단순 매치 개수입니다. 같은 수치 `30분`을 세 번 반복해도 통과합니다. 이 게이트가 발행 품질을 막는 역할이면 쉽게 우회되어 저품질 콘텐츠가 통과합니다.  
권장: **수정**. `new Set(matches.map(normalize))`로 고유값 기준 집계하세요.

[Med] [scripts/check-content.mjs](/C:/Users/노하우셀러/authenticart/scripts/check-content.mjs:38)  
내부링크도 단순 개수라 동일 링크 2회 반복으로 통과합니다. “고아 페이지 방지” 목적이면 서로 다른 내부 URL 수를 봐야 합니다.  
권장: **수정**. URL을 추출해 정규화한 뒤 unique count로 검사하세요.

[Low] [scripts/check-content.mjs](/C:/Users/노하우셀러/authenticart/scripts/check-content.mjs:45)  
FAQ 메시지는 `정확히 4개`라고 하지만 조건은 `< 4`라서 5개 이상은 통과합니다. 운영자가 “정확히 4개”로 이해하고 콘텐츠를 맞추면 검사 결과와 기준이 어긋납니다.  
권장: **수정**. 실제 기준이 최소 4개면 메시지를 `최소 4개`로 바꾸고, 정확히 4개가 맞으면 `post.faq.length !== 4`로 바꾸세요.

[Low] [scripts/publish-quota.mjs](/C:/Users/노하우셀러/authenticart/scripts/publish-quota.mjs:12)  
미추적 파일의 `WEIGHTS`가 절대 경로에 고정되어 있습니다. 다른 PC/CI/Claude 실행환경에서 조용히 `rate=null`로 떨어지고 `quota: 1` 발행 모드가 됩니다. 측정 파일 접근 실패와 실제 미측정을 구분하지 않아 운영 판단이 흐려집니다.  
권장: **수정**. env var 또는 repo 기준 설정으로 받고, 파일 없음/JSON 파싱 실패/브랜드 없음은 reason에 구분해서 출력하세요.

**수정 판단**

- `.claude/settings.local.json`: **수정 필요**. `mcp__claude_ai__transactions`는 제거 쪽이 안전합니다.
- `scripts/check-content.mjs`: **수정 필요**. 고유 수치/고유 내부링크/FAQ 기준 불일치는 운영 게이트 품질 저하입니다.
- `scripts/publish-quota.mjs`: **보류 후 수정**. 커밋 전 인코딩과 경로/실패 사유 처리를 정리해야 합니다.

“공개 금지” 수준의 코드/API 키/DB 접속정보 노출은 diff 자체에서는 보이지 않았습니다. 다만 transactions 권한은 민감정보 접근면이라 그대로 두는 것은 권장하지 않습니다.