<!-- Codex 코드리뷰 | repo: C:\Users\노하우셀러\authenticart | 생성: 2026-07-19 09:05 -->

[변경 요약]
 .claude/settings.local.json |  4 +++-
 scripts/check-content.mjs   | 25 ++++++++++++++++++++++---
 scripts/publish-blog.mjs    | 14 ++++++++++++++
 3 files changed, 39 insertions(+), 4 deletions(-)

**리뷰 결과**

1. **[High] 중복 발행 방지 로직을 우회하는 Storage 부작용**
   - 위치: `scripts/publish-blog.mjs:55-58`, `scripts/publish-blog.mjs:73-75`, `scripts/embed-body-images.mjs:80-85`
   - `blog_posts`는 `ignoreDuplicates: true`라서 같은 `slug`는 DB 갱신되지 않지만, 그 전에 `embedBodyImages()`가 실행되고 `upload(..., { upsert: true })`로 `slug-b1.png`, `slug-b2.png`를 덮어씁니다.
   - 결과적으로 “중복 발행은 무시”라는 DB 의미와 달리, 기존 공개 글의 이미지 파일만 조용히 바뀔 수 있습니다. 감사 로그도 없고 DB row도 안 바뀌어서 추적이 어렵습니다.
   - 권장: 기존 slug를 먼저 조회해 신규 글만 이미지 생성하거나, Storage key를 content hash/발행 batch id 기반으로 만들고 DB insert 성공 후에만 업로드하세요.

2. **[Med] 이미지 삽입 실패를 경고만 남기고 운영 DB 발행을 계속함**
   - 위치: `scripts/publish-blog.mjs:55-63`
   - 이미지 생성/업로드 실패 시 `console.warn` 후 그대로 upsert합니다. 원문에 `[[IMG: ...]]` 마커가 남아도 `status: 'published'`로 운영 DB에 들어갈 수 있습니다.
   - 이는 보안 취약점보다는 데이터 무결성/콘텐츠 품질 문제지만, 서비스롤로 직접 운영 DB에 쓰는 경로라 실패 정책은 엄격해야 합니다.
   - 권장: 마커가 있는 글에서 이미지 변환 실패가 발생하면 전체 발행을 `process.exit(1)`로 중단하거나, 실패한 post를 rows에서 제외하세요.

3. **[Med] 공개 Storage 버킷의 쓰기/삭제 권한이 객체 범위 없이 넓음**
   - 위치: `supabase/migrations/0049_blog_covers_bucket.sql:23-42`
   - `admin`, `branch_manager`가 `blog-covers` 버킷 전체 객체를 insert/update/delete 할 수 있습니다. 객체 key prefix, 작성자, 블로그 slug 소유권 검사가 없습니다.
   - 현재 앱에서 `branch_manager`가 블로그 관리 권한을 갖는 패턴은 보이지만, Storage 객체 삭제는 공개 글 이미지 파손으로 직결됩니다.
   - 권장: 실제 UI 업로드가 필요 없다면 authenticated 쓰기 정책을 만들지 말고 service role 전용 운영 스크립트로만 제한하세요. UI 업로드가 필요하면 `blog/<slug>/...` 같은 prefix와 blog 관리 권한을 묶어 검증하세요.

4. **[Low] `.claude/settings.local.json` 권한 확장은 커밋 대상에서 분리하는 게 안전함**
   - 위치: `.claude/settings.local.json`
   - `mcp__claude_ai__transactions`, `Artifact` 허용 추가는 앱 런타임 취약점은 아니지만 로컬 에이전트 권한 표면을 넓힙니다.
   - 특히 `.claude`는 사용자의 기준 구현이라고 명시되어 있으므로, 프로젝트 변경과 섞어 커밋하지 않는 편이 좋습니다.

5. **[Low] `check-content`의 숫자 검사는 한국어 단위/소수/퍼센트 누락 가능성이 큼**
   - 위치: `scripts/check-content.mjs:35-36`
   - 현재 정규식은 특정 단위만 잡습니다. “3.5시간”, “20%”, “2~3명”, “1만원대” 같은 SEO상 유용한 구체 정보가 누락될 수 있습니다.
   - 보안 문제는 아니고 콘텐츠 게이트 품질 문제입니다. Claude 프롬프트 게이트로 쓰려면 false positive/negative 예시 테스트를 붙이는 게 좋습니다.

**질문/확인 필요**

`branch_manager`가 블로그 전체 이미지까지 삭제/교체할 수 있는 것이 의도인가요? 의도라면 3번은 낮은 운영 리스크로 내려갈 수 있습니다. 다만 1번과 2번은 의도와 무관하게 발행 데이터 무결성 문제라 수정 권장입니다.

`node --check`는 변경/신규 스크립트 6개 모두 통과했습니다. 코드 수정은 하지 않고 리뷰만 수행했습니다.