<!-- Codex 코드리뷰 | repo: C:\Users\노하우셀러\authenticart | 생성: 2026-07-18 09:06 -->

[변경 요약]
 .claude/settings.local.json |  3 ++-
 scripts/check-content.mjs   | 25 ++++++++++++++++++++++---
 scripts/publish-blog.mjs    | 14 ++++++++++++++
 3 files changed, 38 insertions(+), 4 deletions(-)

검토만 했고 파일은 수정하지 않았습니다.

**Findings**
- **[High] 중복 slug 발행 시 기존 본문 이미지가 조용히 덮어써질 수 있음**  
  [scripts/publish-blog.mjs](C:/Users/노하우셀러/authenticart/scripts/publish-blog.mjs:58)에서 DB upsert 전에 이미지를 생성·업로드하고, [scripts/publish-blog.mjs](C:/Users/노하우셀러/authenticart/scripts/publish-blog.mjs:75)는 `ignoreDuplicates: true`입니다. 그런데 [scripts/embed-body-images.mjs](C:/Users/노하우셀러/authenticart/scripts/embed-body-images.mjs:83)는 `upsert: true`, [scripts/gen-cover.mjs](C:/Users/노하우셀러/authenticart/scripts/gen-cover.mjs:28)는 slug 기반 고정 키(`slug-b1.png`)를 씁니다.  
  결과: 같은 slug를 다시 발행하면 DB row는 갱신되지 않지만 Storage 객체는 덮어써져 기존 글의 이미지가 바뀔 수 있습니다. “중복 발행 방지” 의도와 실제 동작이 다릅니다.  
  **Claude 수정 지시:** 이미지 생성 전에 기존 slug를 조회해 중복 slug는 이미지 업로드도 건너뛰게 하거나, DB insert 성공이 확인된 신규 글에 대해서만 이미지 업로드를 수행하게 변경. 가능하면 `upsert: true` 대신 신규 발행별 unique key를 쓰고 실패 시 orphan cleanup 경로도 추가.

- **[Med] `branch_manager`가 `blog-covers` 전체 객체를 직접 수정/삭제 가능**  
  [supabase/migrations/0049_blog_covers_bucket.sql](C:/Users/노하우셀러/authenticart/supabase/migrations/0049_blog_covers_bucket.sql:23)부터 insert/update/delete 정책이 `admin`, `branch_manager` 모두에게 버킷 전체 권한을 줍니다. Supabase Storage API는 앱 UI를 우회해 직접 호출할 수 있으므로, 지부관리자가 전역 블로그 이미지 객체를 덮어쓰기/삭제하는 RBAC·IDOR 표면이 생깁니다. 공개 이미지라 PII급은 아니지만 운영 콘텐츠 훼손 위험은 있습니다.  
  **Claude 수정 지시:** 블로그 이미지 버킷 쓰기는 `admin`만 허용하거나, `branch_manager`가 꼭 필요하면 `name` prefix와 소유자/지부 매핑 조건을 `WITH CHECK`/`USING`에 추가.

- **[Med] 이미지 생성 실패가 발행을 계속 진행해 원본 마커 또는 orphan 이미지가 남을 수 있음**  
  [scripts/publish-blog.mjs](C:/Users/노하우셀러/authenticart/scripts/publish-blog.mjs:57)에서 `embedBodyImages` 실패를 catch 후 계속 발행합니다. `embedBodyImages`는 마커별로 순차 업로드하므로 [scripts/embed-body-images.mjs](C:/Users/노하우셀러/authenticart/scripts/embed-body-images.mjs:102) 이후 일부 업로드가 성공한 뒤 다음 이미지에서 실패하면, DB에는 원본 `[[IMG: ...]]`가 발행되고 Storage에는 참조되지 않는 이미지가 남을 수 있습니다.  
  **Claude 수정 지시:** 마커가 있는 글에서 이미지 생성 실패 시 해당 post 발행을 막거나, 실패 시 업로드된 객체를 삭제하고 명시적으로 “이미지 없는 발행 허용” 플래그가 있을 때만 계속 진행.

**나머지**
- [scripts/check-content.mjs](C:/Users/노하우셀러/authenticart/scripts/check-content.mjs:16)의 콘텐츠 게이트 강화는 보안/API/DB 리스크는 낮습니다. 다만 발행 실패율이 올라갈 수 있으니 운영 정책 변경으로 보면 됩니다.
- [.claude/settings.local.json](C:/Users/노하우셀러/authenticart/.claude/settings.local.json:22)의 `mcp__claude_ai__transactions` 허용은 repo 런타임 보안 이슈는 아니지만, Claude 권한 표면이 넓어지는 변경입니다. 이 권한이 실제로 필요한 작업에만 쓰이는지 확인 대상입니다.